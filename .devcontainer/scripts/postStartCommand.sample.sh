#!/bin/sh
# Runs inside the container every time it starts.
# Copied to postStartCommand.sh by initializeCommand.sh; edit that copy, it is gitignored.
set -e

# Add project setup here, e.g. dependency installs.
